# Copy this file to android/keystore.properties and fill in real values.
# NEVER commit the real keystore.properties or the .jks/.keystore file to git.
storeFile=../meditrack-release.jks
storePassword=CHANGE_ME
keyAlias=meditrack
keyPassword=CHANGE_ME
